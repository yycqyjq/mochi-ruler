# 更新日志

本项目的所有重要变更记录于此。版本遵循语义化版本（主版本.次版本.修订号）。

## [未发布]

### 修正

- 趋势带的 hover 十字线与提示框**从未显示过**：`.trend-cross` / `.trend-tip` 的
  `display:none` 写在了基础规则里，而 JS 是靠 `hidden` 属性切换的——作者样式
  优先级高于浏览器默认的 `[hidden]`。改挂到 `[hidden]` 上；顺带修掉
  `var(--rule-strong)` 这个从未定义的变量（全站用的是 `--line-strong`），
  否则十字线即便显示也没有颜色
- 「只看有违规」按钮一点就崩：`const list` 紧接着被重新赋值，抛
  `TypeError: Assignment to constant variable`——fixbar 的违规芯片与「复制逐章」
  走同一路径
- 复制出去的逐项表缺少「·人味」这类维度后缀（`||` 与 `+` 的优先级问题）
- 同一个文件 / 文件夹选第二次不触发（`input.value` 未重置）
- **拆章只认单个 `#`，`## 第N章` 会被并成 1 章**：语料里大量文件是
  「`# 卷标题` / `## 第N章`」两级 Markdown 结构，实测某标准目录 46 个文件里
  45 个受影响（该目录 53 章 → 523 章）。改为支持 1–6 级 Markdown 标题。
  ⚠ 真人侧拆章**逐字不变**，故基准与阈值不受影响；但这项修正会让部分指标的
  分离度数字**下降** —— 那是旧数字虚高（AI 侧被大块平均、真人侧是正常章长，
  粒度不匹配会系统性抬高分离度），修正后才是真实区分力

### 体验

- 键盘可达性：五维卡 / 雷达顶点 / 逐章行 / 「显示全部」都能 Tab 到并用
  Enter / Space 操作，读屏能播报选中态与展开态。雷达 svg 的 role 由 img 改为
  group（img 是叶子节点，会把里面可聚焦的顶点整个从无障碍树里抹掉）。
  真浏览器验证时又抓出两处：五维卡按 Enter 后焦点会随重渲染丢失（现已还回
  焦点），逐章行的 `aria-expanded` 因走的是直接改 DOM 的路径而从不更新
  （现已手动同步）
- 后端报错不再被吞：非 2xx 时先读 body，把 `{"error": "..."}` 里的真实原因
  （如「内容太短」）显示出来，而不是只报「检测失败：500」
- 文件读取失败不再卡死：原来任一文件读失败会让 UI 永远停在「读取中…」，
  现在会跳过并统一弹窗点名（最多列 8 个）
- 趋势带 hover 更跟手：`getBoundingClientRect` 缓存 200ms（原来每次 mousemove
  都调，强制重排）、最近点查找改二分（1200 点时从 1200 次比较降到约 11 次）、
  mousemove 用 rAF 节流

### 工程

- 新增 `tools/static_check.py` 并接进 CI：扫三类**静默故障**——CSS 变量用了却
  没定义、JS `const` 声明后又赋值、前端硬编码的项数与 `server.SHOW` 不符。
  这三类本次各踩过一次，都是浏览器不报错、只是功能失效的那种

## [1.1.3] - 2026-09-22

### 新增

- 逐章趋势带：总分折线 + 朱砂标杆虚线，纵轴随数据自适应；点击 / 触摸
  折线直接定位到对应章行并展开明细，千章书按窗口聚合（回答「从第几章
  开始掉」——数字表看不出趋势，折线一眼可见）
- 上次检测对比（ghost）：汇总层存 localStorage，总分卡 / 五维卡 / 40 项
  尺条出现「上次位置」灰点与 ▲▼ 涨跌，雷达叠上一版的灰色五边形——
  改稿重测的涨跌闭环（首次检测自动不显示）
- 雷达交互化：顶点点击 = 维度筛选（与五维卡一致）、环线补 2.5 / 5 / 7.5 /
  10 刻度值、顶点悬浮显示与标杆的差距
- 窄屏（≤960px）检测完成后自动滚动到结果区

### 修正

- 亮色主题判定色对比度不足（good 3.97 / warn 3.25 / bad 4.42，AA 正文
  要求 ≥ 4.5）：--good #27704a、--warn #855914、--bad #a63b2a（暗色主题
  已达标，不动）
- 雷达图维度标签在 190 视框两端被裁切（实测占位 −2.1…192.2）：viewBox
  扩边留白
- 移动端逐章表把页面撑出 182px 横向滚动（6×56+52 固定列未随断点收窄）：
  640px 以下改为表内横向滚动
- 触控目标过小（实测最小 15px）：主题开关 / 来源移除 / 口径气泡扩大
  命中区

## [1.1.2] - 2026-09-21

### 修正

- 两项词表扩容，减少「真人的写法没被认出来」的漏判（分离度与左尾同步改善）：
  - **意外/示证**（代入感维）：补入裸「竟」与 7 个多字短语（岂料 / 出乎意料 /
    始料未及 等）；裸「竟」排除「毕竟 / 究竟」。项级分离度 0.71 → 0.75
  - **情绪词种类**（人味维）：补入 31 个**二字词**（欣慰 / 无奈 / 尴尬 / 疲惫 等）。
    刻意不用单字穷举——单字会撞上音译人名、战队名与同形词，增益是假的。
    项级分离度 0.92 → 0.95，该维真人最低分 4.98 → 5.05、「低于 5 分」本数 1 → 0
  - **情绪词密度**（人味维）：`emo` 与 `emo_type` 统一到同一套词表（原 18 单字
    ∪ 31 个二字词）。项级分离度 0.68 → 0.73。**阈值刻意不动**——标定给出的
    k=0.85 建议线实测压左尾 0.47，沿用旧线（在新分布下相当于 k≈0.79）左尾
    反而改善 0.37。两项相关性 0.145 → 0.166，仍是独立轴
  - 阈值随口径重标定（`surprise` / `emo_type` 两项），25 本基准表重建
    （其余 1000 个基准值逐位未变）
  - 验证：零回归通过（未预期漂移 0 处，真人感 / 节奏 / 句法 / 代入感四维零漂移）、
    逐目录泛化 5 个 AI 目录无一倒退、无语料自测 29/29

### 工程

- CI 与发布流水线的 action 升到 node24 运行时（checkout v5 / setup-python v6 /
  setup-node v5 / upload-artifact v6），消除 GitHub 的 Node 20 弃用告警——
  此前靠强制降级才没红，兼容开关一撤构建即失败。注意 `upload-artifact@v5`
  仍跑 node20，必须 v6 起
- CI 工作流补 `workflow_dispatch`，排查时可直接手动空跑，无需推 commit
- 新增 `.github/dependabot.yml`：月度巡检 action 版本，避免同类过期问题
  再次只能靠人工翻日志发现
- `desktop/package-lock.json` 版本字段与 `package.json` 对齐（1.0.0 → 1.1.1）

## [1.1.1] - 2026-09-17

### 修正

- macOS 应用打开报「已损坏」：构建产物补做 ad-hoc 签名（无证书环境下
  Gatekeeper 会拒绝未签名应用）；真机验证签名校验通过、应用与评分核心
  均可正常启动，首次打开按「右键 → 打开」即可
- 应用图标替换为透明底标准样式（按 macOS 留白与圆角比例重制，替换此前
  带白色底衬、边缘不透明的版本）

### 工程

- 发布流水线：手动触发的空跑构建改为产出完整安装包（此前没有可供下载
  核验的产物），并在构建后自动校验 macOS 签名

## [1.1.0] - 2026-09-17

### 新增

- 桌面版（Electron + PyInstaller sidecar）：应用名「墨尺」，push `v*` tag 自动在
  GitHub Actions 云端构建 macOS（arm64/x64 .dmg）与 Windows（.exe 安装器）并发布
  到 Release；壳只负责 spawn 评分核心与开窗口，评分口径与浏览器版一致；
  未签名发布（macOS 右键打开 / Windows 过 SmartScreen）
- 五维雷达图（你 vs 标杆）与逐章分数格热力底色
- 检测请求 `light` 瘦身（1000 章响应体 -44%）与 HTTP/1.1 keep-alive

### 修正

- 来源标签不再自估章节数（前端估算与后端拆章选型口径不一致），统一以检测结果为准

## [1.0.0] - 2026-09-17

首个正式版本。

### 评分核心

- 五维评分（真人感 / 人味 / 代入感 / 节奏 / 句法），40 项指标，0–10 分、
  越高越不像 AI；逐项分 → 维度加权 → 五维等权总分
- 匿名基准 25 本（全书等距抽样 100 章/本，只存原始指标中位，运行时用当前
  公式算分，口径永远自洽）
- 合规硬规则（圆括号注记 / 违禁元词 / 破折号超限 / 正文加粗 / 章内重复 /
  群像 / 章末抒情）与逐章报告
- 指标入库纪律：残差检验（防句长回声）、排版噪声排除、引号碎片检验、
  左尾体检（防风格税）、留一目录泛化；沿革见 docs/methodology.md

### 界面

- 「纸墨」视觉改版：印章 logo、带刻度的尺度评分条（每格 1 分）、朱砂标杆线、
  明暗双主题、零依赖
- 「最该先改」摘要条：自动列出差距最大的维度 / 指标与硬规则命中，点击直达
- 逐项对比：按维度分组 / 按差距排序双模式，每行带「尺」列；40 项全部配有
  口径说明气泡（悬停 / 键盘 / 触屏）
- 逐章钻取：章行展开该章最差项与违规完整详情；原文顺序 / 总分最低排序、
  「只看有违规」过滤、大书（> 60 章）截断渲染
- 章标题渲染前 HTML 转义（章头行可能来自任意来源的 txt，防注入）
- 短文本（< 1000 字）统计不稳警告；违规名芯片共用口径气泡
- 复制表格（汇总 + 逐项）与复制逐章两个视图，均与屏幕所见同源

### 输入与健壮性

- 自动识别 GBK / GB18030 / UTF-16 编码（UTF-8 严格解失败后回退），
  转码在来源标签中点名提示
- 载入文件不再清空输入框；检测请求 120 秒超时提示
- `/api/analyze` 请求体上限 64MB（超限 413）、非法 Content-Length 返 400；
  端口参数非数字时友好报错

### 工程

- `tools/selftest.py`：29 项无语料自测（拆章 / 阈值 / 47 键完整性 / API 冒烟）
- GitHub Actions：Python 3.9 / 3.13 双版本跑自测与编译检查
- 开发工具箱：audit（区分度体检）/ calibrate（阈值标定）/ bench_build（重建
  基准）/ layout_scan（排版噪声）/ sent_scan（引号碎片）/ expand_eval（加指标
  余量）/ regress（零回归）/ narrative（叙事诊断视图）
