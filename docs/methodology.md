# 标定方法论与沿革

> 本篇内容原在 README 的「整轮回标定」一节，2026-09-17 **原样搬入**（未改写
> 任何数字与结论），供追溯历史。⚠ 文中订正摞着订正，**每处最后一条订正才是
> 当前生效的结论**；当下口径一律以 `qc_core.py` 各阈值表上方的注释与
> README「改指标前必读」的六条硬性要求为准。

### 整轮回标定（2026-09-14 已执行）

**结论先行：42 项里 40 项标「← 需更新」，但绝不能机械套用。** 实测把 38 项建议值一次性代入，真人感维的真人逐本最低分从 8.50 砸到 **7.50**——多数项的 `over`（超标线）被收紧到 AI 中位，**等于取消原来的宽限带**，本想更准，结果更伤真人。

改走**逐项筛**：每次只改一项，判据 = **`sep` 涨 且 真人逐本最低分不降**。逐项 Δ 的分半信度 `r(Δ半A, Δ半B) = +0.672`（n=38，符号一致 29/38）——**是可靠信号，不是噪声**；问题不在真假，在大小。

**本轮采纳 10 项阈值**（`breath` 曾入选，因会让该维 `sep` 反降 0.0005 而剔除）：

| 项 | 维 | 旧 → 新 |
|---|---|---|
| `sent_p90` | 节奏 | `(65.0, 30.0)` → `(50.8, 27.0)`　**单项收益最大** |
| `simile` | 真人感 | `(1.00, 2.50)` → `(1.358, 3.724)` |
| `sent_den` | 真人感 | `(35.0, 70.0)` → `(37.89, 71.73)` |
| `dash` | 真人感 | `(0.50, 6.00)` → `(0.7568, 5.045)` |
| `dem_lit` | 真人感 | `(1.40, 0.00)` → `(1.177, 0.00)` |
| `emo` | 人味 | `(3.58, 1.62)` → `(3.665, 1.545)` |
| `redupl` | 人味 | `(5.577, 2.911)` → `(5.568, 2.854)` |
| `emo_type` | 人味 | `(5.02, 0.9195)` → `(5.016, 0.9195)` |
| `pron3` | 句法 | `(12.00, 17.50)` → `(11.11, 19.68)` |
| `pron_start` | 句法 | `(0.045, 0.080)` → `(0.03208, 0.09449)` |

**并清退代入感三弱项**：`imm_cog`（认知反应）/ `imm_perc`（感知）/ `imm_soma`（身体感受）——`sep` 只有 0.23–0.31，却各压 **7–10 本**真人到 5 分以下，权重合计占该维 23%。判据是「**`sep` 低 + 压真人**」，缺一不可。机制：三项在**真人内部互相打架**（同一本书在一项上高分、另一项上低分），加权平均后把真人侧拉平，而 AI 侧本来就在低位集中——于是维 `sep` 反被拖低。**改阈值救不了**：`sep` 是原始分布的性质，建议线只挪 0.02 级。项数 42 → **39**。

**效果（五维全部双升）**：

| 维 | `sep` | 真人逐本最低分 |
|---|---|---|
| 真人感 | 0.9877 → **0.9903** | 8.50 → 8.72 |
| 人味 | 0.9406 → **0.9419** | 4.96 → 4.98 |
| 代入感 | 0.9239 → **0.9339** | 4.95 → **5.23** |
| 节奏 | 0.9713 → **0.9799** | 5.50 → **6.21** |
| 句法 | 0.9976 → **0.9988** | 6.98 → 7.15 |
| 总分 | 1.0000 → 1.0000 | 6.97 → **7.34** |

**收益高度集中在两处**：代入感三弱项退场 + `sent_p90` 重标定，合计贡献总分增益的 **86%**；其余 9 项只多 +0.05，撑起的是真人感 / 人味 / 句法三维的增益（按去退化目录口径各 +0.0005 / +0.0014 / +0.0011，见下方泛化验证的订正）。

**泛化验证（留一目录，同日补做）**：本轮阈值是在这 5 个 AI 目录上标定的，必须验「换一批 AI 文本还准不准」。做法：每次留出 1 个目录、用其余 4 个重新标定阈值（同 `k=0.85` 口径），在**留出目录**上测 Δsep。

- **留一均值与「仅 4 目录的 in-sample」同号同量级** → 阈值不挑目录：

| 维 | 留一均值（剔退化折） | 仅 4 目录 in-sample |
|---|---|---|
| 真人感 | +0.0006 | +0.0005 |
| 人味 | +0.0016 | +0.0014 |
| 代入感 | +0.0169 | +0.0100 |
| 节奏 | +0.0017 | +0.0020 |
| 句法 | +0.0014 | +0.0011 |

- **逐目录无一项被改坏**：落盘表在 5 个目录上的 Δsep，最差是「AI_DIR_C」的代入感 **−0.0049**，其余全部为正。
- **真人侧左尾全为正**（取每本维度分最低的一本）：真人感 +0.21 / 人味 +0.02 / 代入感 +0.28 / 节奏 +0.71 / 句法 +0.17 / 总分 +0.36。
- ⚠ **订正上表两处数字**：`AI_DIR_B` 只有 **3 块**，sep 本身不可信，却系统性夸大了两项收益——节奏 `+0.0085` 里有 **+0.0066（78%）**、真人感 `+0.0027` 里有 **+0.0022（81%）** 来自它。**只按 4 个正常体量目录算，真实收益是节奏 +0.0020、真人感 +0.0005**。代入感 / 人味 / 句法不受影响（贡献 ≈0），可照原数字采信。

**未采纳，留档备查**：

- **27 项逐项判定为「不采纳」**。其中两个是「赚 `sep` 但压真人」的大项：`cv`（+0.0050 / **−0.42**）、`para_cv`（+0.0072 / **−0.32**）——正好印证判据。
- **八个死项**：`bold` / `tell` / `dede` / `rev` / `onomat` / `space` / `simile` / `comma_in`——真人侧 24 本全满分，清退它们不救任何人（`sep` 只 +0.0067）却要付真人最低分 −0.28 的代价。其中 `simile` 本轮另有阈值重标定，与「清退不采纳」是两件事，互不冲突。
  - ⚠ **2026-09-15 复核订正**：**结论（不清退）不变，但理由要改写**——「24 本全满分」和那两个数字都复现不出来。
    - ⚠⚠ **本条自身也有两处错，2026-09-15 二次订正（见下条）**：一是只扫了名单里的 8 项、漏了 `tail`/`dash`/`nego`；二是判据只用了「真人全满分」、漏了「`sep` 低」，把 `tell`/`dede` 误判成死项。
    - 复核方法：逐本中位与 `qc_core.BENCHMARKS` 对账 **240/240 逐位吻合**（先复现官方基线再下结论）；阈值经 `git show` 逐版比对，除 `simile` 外一字未改。原始记录的 `+0.0067 / −0.28` 用两个状态都复现不出、成因不可考，故**不覆写原数字**，只在此订正。
    - **清退是净损失，不是「不救任何人」**：39 项状态下清退这 8 项，五维 `sep` 合计 **−0.0013**、真人逐维最低本合计 **−0.66**；在当轮的 42 项状态下复跑同样是 **−0.0035 / −0.89**。其中 `comma_in` 一项就占 **−0.0036 / −0.66**——它**是节奏维左尾的支柱**（13 本真人靠它拿满分），不是空转项。
    - ⚠ 顺带一条**口径澄清**：`comma_in` 的算法是 `body.count("，") / k`（`k` = 去空白字数/1000），它本来就是**「逗号密度/千字」**，不是「句内逗号数」。所以它是「标点密度」的**一半**，且与另一半（非逗号标点）**方向相反**——真人逗号更多（55.1 vs 27.9），真人标点总量更少（100.8 vs 137.0）。
- **死项名单（2026-09-15 二次订正，全 39 项扫一遍）**：
  - **真人侧 24/24 全满分的共 6 项**：`tail` / `bold` / `dash` / `tell` / `nego` / `dede`（与阈值版本无关，`bf25d97` 与当前阈值下都是 24/24）。上一轮只查了名单里的 8 项，漏掉 `tail`/`dash`/`nego`。
  - **但「真人全满分」只是必要条件**。按本仓判据（「`sep` 低 **且** 真人侧全满分」），**真正的死项只有 2 个**：
    - `bold` `sep` **0.22**、`tail` `sep` **0.18** —— 真死项（AI 侧也 78% / 82% 满分）
    - `tell` **0.60** / `dede` **0.61** —— 真人全满分但 `sep` 不低，是**宽限带**，不是死项
    - `dash` **0.96** / `nego` **0.88** —— 是**区分度主力**（AI 侧满分率只有 **4% / 12%**），清退必亏
  - **清退实测**（`.workbuddy/notes/probes/mochi_deaditems.py`）：清退真死项 `bold`+`tail` → 真人感 `sep` **+0.0002**（噪声地板 +0.0008 的 1/4）、真人最低本 **−0.15**；把 6 项全满分项一起清 → `sep` **−0.0055**、左尾 **−0.67**。**结论：不清退。**
  - 机制：恒满分项是**免费的 10 分垫片**，`_wavg` 归一后删掉它，所有人的分一起下移、左尾下移更多。所以「删死项只省计算、不伤人」是错的。
- **AI 组口径不统一**：部分阈值的历史记录用 129 块口径、部分是 185 块（多收了两个语料目录）。本轮统一按 185 块复算，但源码注释里的 129 块数字未逐一改写，读注释时注意区分。
- **那个 3 块目录的「真人感盲区」不存在**（2026-09-15 扩样复核，探针 `mochi_expand_ai.py`）。
  - 上一轮记录里该目录的真人感 `sep` 0.115 / 0.304 **复现不出**：复核时（39 项口径）真人感 `sep` = **1.000**、**0/185 块越界**（`punc_den` 只进节奏维，真人感维不受影响，40 项口径下同值）。查因——该来源**只有 3 章**（2154 / 2041 / 1920 字），没有纵向扩样空间；旧数字是「旧口径 + 3 块小样本」的产物，不是真盲区。
  - 改为**横向铺开**（逐目录算边际）后：**真人感维 0/185 块越界、总分 0/185**。
  - 真正有余量的是**人味（9/185 ≈ 5%，5/5 目录边际为负）与代入感（12/185 ≈ 6%）**——这两个维才是后续加指标该盯的方向，不是真人感。
  - 口径：**边际** = 最低真人本分 − 该目录 AI 最高分；**越界块** = 维度分 ≥ 最低真人本 的块数。`sep` 贴天花板时没有分辨力，要看边际。
  - ⚠ **一致粒度复核订正（2026-09-15 续，探针 `mochi_expand_ai_v2.py`）**：v1 的越界用「真人 24 本 dim_score 最值」对照「AI 185 章块」——**粒度不一致（铁律 7）**，真人侧是平滑后的按本聚合值，阈值被抬高，把越界**低估 3-4 倍**。把两侧都拉到章块级（真人 2400 章 vs AI 185 章）重算：
    - 章块级 sep：真人感 **0.99** / 人味 **0.94** / 代入感 **0.93** / 节奏 0.99 / 句法 1.00 / 总分 1.00。
    - 越界块（章块 P05 阈值 =「比 95% 真人章还像真人」）：真人感 **3** / 人味 **34** / 代入感 **36** / 节奏 5 / 句法 0 / 总分 0，合计 **78**（v1 按本最值口径 24）。
    - 判读：① 真人感「盲区不存在」**成立**（3/185 仍近 0，sep 0.99），只是量级从 0→3；② 人味/代入感「有余量」方向对，但**量级被 v1 低估 3-4 倍**（9→34、12→36，即 18%/19%）。
  - ⚠ **加指标方向订正（C3，探针 `mochi_c3_cand.py`）**：人味/代入感 sep 0.93/0.94 **已接近天花板**，并非「还有未开发的轴」——`emo`/`breath`/`touch_temp` 等已占满信号。实测 3 个未覆盖的内容轴候选（感官词密度 / 广义身体感受 / 内心活动词），sep 仅 **0.09 / 0.35 / 0.53**，全低于该维加权平均 sep（人味≈0.94、代入感≈0.93），按铁律 4 加进去都是净损失；唯一真独立轴 `inner_den`（与现有人味项最大相关仅 0.064、Δ维sep +0.0151）**压 5 本真人左尾**、且 AI 中位 0 是这几个语料的极端特性、泛化存疑，净收益被抵消。**结论：这两个维无「加项提 sep」的净空间，维持不动（A 方案）；真实 headroom 在左尾——18%/19% 的 AI 章嵌进真人分布内部，靠「换轴覆盖这批 AI 章的新失败模式」而非加项。**

复现脚本（未入库，都在 `/tmp`）：`mochi_fullrecal.py`（全量套用预演）、`mochi_peritem.py`（逐项判定）、`mochi_splithalf.py`（分半信度）、`mochi_package.py`（推荐包复算）、`mochi_marginal.py`（边际拆解）、`mochi_loo.py`（留一目录泛化 + 退化目录贡献拆解）。

2026-09-15 的外部模块评估、「死项复核」、「引号污染三方案」与「扩样复核」脚本改放 `.workbuddy/notes/probes/`（`/tmp` 会被清）：
`mochi_ext23_probe.py`（17 个候选的 sep / 残差 / Δ维sep）、`mochi_ext23_corr.py`（逐块对齐真相关 + 退化目录拆解）、`mochi_ext23_extra.py`（逐目录 sep）、`mochi_ext23_defects.py`（外部代码 6 处缺陷复现）、`mochi_route_abc.py`（`punc_den` 五条走法 + 死项复核）、`mochi_punc_split.py`（标点密度拆成逗号 / 非逗号两半）、`mochi_pronstart_abc.py`（引号污染三方案的**维级**实测）、`mochi_deaditems.py`（死项二次复核：全 39 项扫 + 清退实测）、`mochi_expand_ai.py`（逐目录边际 + 越界块数：真人感盲区复核）、`mochi_expand_ai_v2.py`（一致粒度越界复核：章块级 sep + P05 阈值）、`mochi_c3_cand.py`（人味/代入感内容轴候选实测：无加项净空间）。
